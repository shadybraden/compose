---
services:
  socks5:
    container_name: socks5
    environment:
      - PROXY_PORT=1080
    image: serjs/go-socks5-proxy:v0.0.3
    depends_on:
      gluetun:
        condition: service_healthy
    networks:
      - proxy

networks:
  proxy:
    external: true
