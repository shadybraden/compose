---
services:
  socks5:
    container_name: socks5
    environment:
      - PROXY_PORT=1080
    image: serjs/go-socks5-proxy:v0.0.3
    networks:
      - proxy
    ports:
      - 1080:1080

networks:
  proxy:
    external: true
