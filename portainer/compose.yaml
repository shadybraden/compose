services:
    portainer:
        container_name: portainer
        restart: unless-stopped
        volumes:
            - /var/run/docker.sock:/var/run/docker.sock
            - /data/config_storage/portainer:/data
            - /etc/localtime:/etc/localtime:ro
        image: portainer/portainer-ce:2.29.2
        ports:
            - 9000:9000
        security_opt:
          - no-new-privileges:true # helps to increase security
        networks:
          - intranet
        environment:
          - CONTAINER_NAME=portainer
        labels:
        - "traefik.enable=true"
        - "traefik.http.routers.${CONTAINER_NAME}.entrypoints=http"
        - "traefik.http.routers.${CONTAINER_NAME}.rule=Host(`${CONTAINER_NAME}.holmlab.org`)"
        - "traefik.http.middlewares.${CONTAINER_NAME}-https-redirect.redirectscheme.scheme=https"
        - "traefik.http.routers.${CONTAINER_NAME}.middlewares=${CONTAINER_NAME}-https-redirect"
        - "traefik.http.routers.${CONTAINER_NAME}-secure.entrypoints=https"
        - "traefik.http.routers.${CONTAINER_NAME}-secure.rule=Host(`${CONTAINER_NAME}.holmlab.org`)"
        - "traefik.http.routers.${CONTAINER_NAME}-secure.tls=true"
        - "traefik.http.routers.${CONTAINER_NAME}-secure.service=${CONTAINER_NAME}"
        - "traefik.http.services.${CONTAINER_NAME}.loadbalancer.server.port=9000" # port of the service. i.e. ${CONTAINER_NAME} likes 9000
        - "traefik.docker.network=intranet"

networks:
  intranet:
    external: true