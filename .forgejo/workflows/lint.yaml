name: Lint

on:
  workflow_dispatch:

jobs:
  Lint:
    runs-on: tools_container
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: https://github.com/docker-compose-linter/dclint-github-action@v1.6.0
        with:
          path: ./
          recursive: true
