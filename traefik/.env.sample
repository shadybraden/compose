CF_TOKEN=cloudflare_token
cf-email=email@domain.tld
TRAEFIK_DASHBOARD_CREDENTIALS=password
# dashboard cred is a hashed password:
# apt install apache2-utils
# echo $(htpasswd -nB admin) | sed -e s/\\$/\\$\\$/g