services:
  plex:
    image: lscr.io/linuxserver/plex:1.41.6
    container_name: plex
#    network_mode: host
    ports:
      - 32400:32400
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - VERSION=docker
    volumes:
      - /data/config_storage/plex/config:/config
      - /mnt/4tb/mcc/shows:/tv
      - /mnt/4tb/mcc/movies:/movies
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true # helps to increase security