---
services:
  cloudflared:
    image: cloudflare/cloudflared:2025.9.0
    command: tunnel --no-autoupdate run --token ${token}
    networks:
      - intranet

networks:
  intranet:
    external: true
