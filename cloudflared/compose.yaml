---
services:
  cloudflared:
    image: cloudflare/cloudflared:2025.8.0
    command: tunnel --no-autoupdate run --token ${token}
    networks:
      - intranet

networks:
  intranet:
    external: true
