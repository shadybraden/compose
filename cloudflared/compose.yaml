---
services:
  cloudflared:
    image: cloudflare/cloudflared:2026.1.2
    command: tunnel --no-autoupdate run --token ${token}
    networks:
      - intranet

networks:
  intranet:
    external: true
