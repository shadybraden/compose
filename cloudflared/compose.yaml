---
services:
  cloudflared:
    image: cloudflare/cloudflared:2026.2.0
    command: tunnel --no-autoupdate run --token ${token}
    networks:
      - intranet

networks:
  intranet:
    external: true
