mqtt:
  enabled: false

ffmpeg:
  hwaccel_args: preset-vaapi

cameras:
  usb_webcam:
    ffmpeg:
      hwaccel_args: ' '
      output_args:
        record: preset-record-generic-audio-aac
      inputs:
        - path: /dev/video0
          input_args: -f v4l2 -framerate 30 -video_size 1920x1080
          roles:
            - record

tls:
  enabled: false
detect:
  enabled: true
version: 0.16-0