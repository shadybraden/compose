---
mqtt:
  enabled: false

cameras:
  frontdoor:
    ffmpeg:
      inputs:
        - path: /dev/video0
          input_args: -f v4l2
          roles:
            - detect
            - rtmp
            - record
