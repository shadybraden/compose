---
mqtt:
  enabled: false

cameras:
  usb_webcam:
    enabled: true
    ffmpeg:
      path: "5.0"
      inputs:
        - path: /dev/video0  # <----- The stream you want to use for detection

tls:
  enabled: false
