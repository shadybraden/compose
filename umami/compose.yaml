---
services:
  umami:
    image: ghcr.io/umami-software/umami:latest
    ports:
      - "61074:3000"
    environment:
      DATABASE_URL: postgresql://umami:${POSTGRES_PASSWORD}@db:5432/umami
      APP_SECRET: replace-me-with-a-random-string
      DISABLE_TELEMETRY: 1
    depends_on:
      db:
        condition: service_healthy
    init: true
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl http://localhost:3000/api/heartbeat"]
      interval: 5s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true  # helps to increase security
    networks:
      - intranet
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.analytics.entrypoints=http"
      - "traefik.http.routers.analytics.rule=Host(`analytics.${DOMAIN}`)"
      - "traefik.http.middlewares.analytics-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.analytics.middlewares=analytics-https-redirect"
      - "traefik.http.routers.analytics-secure.entrypoints=https"
      - "traefik.http.routers.analytics-secure.rule=Host(`analytics.${DOMAIN}`)"
      - "traefik.http.routers.analytics-secure.tls=true"
      - "traefik.http.routers.analytics-secure.service=analytics"
      - "traefik.http.services.analytics.loadbalancer.server.port=3000"  # port of the service.
      - "traefik.docker.network=intranet"
      - "homepage.group=Other"
      - "homepage.name=analytics"
      - "homepage.icon=/images/umami.png"
      - "homepage.href=https://analytics.${DOMAIN}/"
      - "traefik.http.routers.SUBDOMAIN-secure.middlewares=tinyauth"

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: umami
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - /data/config_storage/umami:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true  # helps to increase security
    networks:
      - intranet

networks:
  intranet:
    external: true
