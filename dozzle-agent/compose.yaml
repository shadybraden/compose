---
name: dozzle
services:
  dozzle:
    image: amir20/dozzle:v10.8.0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /data/config_storage/dozzle:/data
    environment:
      - DOZZLE_ENABLE_ACTIONS=true
      - DOZZLE_NO_ANALYTICS=true
    command: agent
    ports:
    - '0.0.0.0:7007:7007'
    networks:
      - intranet
    security_opt:
      - no-new-privileges:true  # helps to increase security

networks:
  intranet:
    external: true
